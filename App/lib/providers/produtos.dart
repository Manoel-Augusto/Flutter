import 'dart:convert';
import 'package:App/components/models/produto.dart';
import 'package:http/http.dart' as http;
import 'package:flutter/material.dart';

/*class Produtos with ChangeNotifier {
  List<Produto> _items = PRODUTO;
  List<Produto> get items => [..._items];

  void addProdutos(Produto produto) {
    _items.add(produto);
    notifyListeners();
  }
}
*/
